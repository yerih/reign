<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.admissions.empty_project.data">

    <application
        android:allowBackup="true"
        android:name="com.admissions.empty_project.EmptyProjectApp"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Empty_project">
        <activity
            android:name="com.admissions.empty_project.ui.NavHostActivity"
            android:exported="true"
            android:theme="@style/Theme.Empty_project.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>